🧩 1. Controller Layer (слой REST API контроллеров)
Отвечает за прием и обработку HTTP-запросов от клиентского приложения, а также за отправку ответов клиенту.

Контроллеры этого слоя:

AuthController

Управляет авторизацией и регистрацией.
login() — авторизация пользователя.
register() — регистрация нового пользователя.
ModelController

Управляет созданием и редактированием экономических моделей.
createModel() — создание новой модели.
analyzeDocument() — отправка документации для AI-анализа.
CalculationController

Запуск расчётов моделей и возвращение результатов.
calculate() — запуск расчёта.
SimulationController

Запускает имитационные модели и возвращает результаты.
runSimulation() — запуск симуляции.
🧩 2. Service Layer (слой бизнес-логики)
Этот слой содержит основную логику приложения и координирует взаимодействие между контроллерами, репозиториями и внешними сервисами (AI).

Классы сервисного слоя:

AuthService

Выполняет авторизацию и регистрацию пользователей.
ModelService

Управляет созданием моделей и взаимодействует с AI-сервисом для анализа документации.
CalculationService

Реализует вычисления экономических моделей.
SimulationService

Выполняет запуск и обработку результатов имитационных моделей.
AIService

Взаимодействует с внешними AI-сервисами, отправляя документацию или финансовую отчётность и получая результаты анализа.
🧩 3. Data Access Layer (слой работы с БД)
Представлен интерфейсами Spring Data JPA репозиториев, которые отвечают за запросы к базе данных PostgreSQL.

Интерфейсы-репозитории:

UserRepository (пользователи)
ModelRepository (экономические модели)
CalculationRepository (результаты расчётов)
SimulationRepository (результаты симуляций)
DocumentRepository (документы, загруженные для AI-анализа)
🧩 4. Domain Entities (доменная модель)
Эти классы отражают структуру данных, хранящихся в БД:

User (пользовательская информация)
Model (экономическая модель с параметрами)
Parameter (параметры моделей)
CalculationResult (результаты вычислений)
SimulationResult (результаты симуляций)
Document (документы для анализа AI)
AIAnalysisResult (результаты анализа документации AI)
🧩 5. DTO Classes (Data Transfer Objects)
DTO используются для передачи данных между клиентом и сервером, защищая внутреннюю структуру системы и обеспечивая чёткий контракт на передачу данных.

Для авторизации:

AuthRequestDTO, AuthResponseDTO, RegistrationDTO, UserDTO
Для моделей:

ModelDTO, ParameterDTO
Для вычислений:

CalculationRequestDTO, CalculationResultDTO
Для имитационного моделирования:

SimulationRequestDTO, SimulationResultDTO
Для документов и AI-анализа:

DocumentDTO, AIAnalysisResultDTO
☁️ 6. External AI Service (внешний AI-сервис)
Это внешний сервис, к которому обращается приложение для анализа документации и финансовых данных, и получения аналитических результатов:

AI_API
Метод analyzeDocumentation() — анализ текстовой документации.
Метод analyzeFinancialReport() — анализ финансовой отчётности.
🔗 Взаимодействие между слоями и классами:
Последовательность вызовов выглядит следующим образом:

Клиент отправляет запрос → REST-контроллер (Controller Layer).
Контроллер вызывает методы бизнес-логики (Service Layer).
Бизнес-логика обращается к данным через репозитории (Data Access Layer).
Репозитории взаимодействуют с БД через сущности (Domain Entities).
Сервисы взаимодействуют с внешним AI-сервисом (AI_API).
Результаты возвращаются обратно через сервисы и контроллеры клиенту в виде DTO.
🎯 Преимущества данной структуры:
Чёткое разделение обязанностей и слоев (Controller → Service → Repository).
Высокая масштабируемость и удобство добавления новых функций.
Лёгкость интеграции внешних AI-сервисов.
Простота поддержки и тестирования каждого компонента отдельно.
Таким образом, представленная диаграмма полностью отражает серверную архитектуру твоего приложения и максимально соответствует описанным ранее требованиям.

Рассуждала для секунда
Ниже приведено подробное объяснение диаграммы классов серверного приложения:

Общая структура
Диаграмма разделена на несколько пакетов, каждый из которых отражает определённый уровень серверного приложения:

Controller Layer (REST API) – предоставляет конечные точки (endpoint) для взаимодействия с клиентом по HTTP.
Service Layer – реализует бизнес-логику приложения, выполняет основные операции и обращения к внешним сервисам.
Data Access Layer (Spring Data JPA) – абстрагирует доступ к базе данных через репозитории.
Domain Entities – отражают основные сущности, хранящиеся в базе данных (например, пользователи, модели, результаты расчётов).
DTO Classes – объекты передачи данных, которые используются для обмена информацией между клиентом и сервером, скрывая внутреннюю структуру доменных сущностей.
External AI Service – внешний модуль, через который происходит анализ документации или финансовой отчетности с помощью AI (например, OpenAI API).
Детальное описание пакетов и классов
1. Controller Layer (REST API)
AuthController
Обрабатывает запросы, связанные с аутентификацией и регистрацией. Методы:

login(AuthRequestDTO): AuthResponseDTO – принимает запрос на вход, возвращает JWT-токен.
register(RegistrationDTO): UserDTO – регистрирует нового пользователя.
ModelController
Отвечает за создание экономических моделей и анализ документации. Методы:

createModel(ModelDTO): ModelDTO – создание новой модели.
analyzeDocument(DocumentDTO): AIAnalysisResultDTO – отправляет документ на анализ и возвращает результаты AI-анализа.
CalculationController
Обрабатывает запросы на выполнение математических расчётов.

Метод calculate(CalculationRequestDTO): CalculationResultDTO запускает расчёты и возвращает результат.
SimulationController
Запускает симуляционные модели и возвращает прогнозные данные.

Метод runSimulation(SimulationRequestDTO): SimulationResultDTO инициирует симуляцию.
Эти контроллеры служат входной точкой для клиентских запросов, преобразуя входные DTO в вызовы сервисного слоя и возвращая результаты в виде DTO.

2. Service Layer
Слой бизнес-логики отвечает за обработку запросов, выполнение основных операций и взаимодействие с внешними системами.

AuthService
Отвечает за аутентификацию и регистрацию. Методы:

authenticateUser(String, String): String – проверяет учётные данные и генерирует JWT-токен.
registerUser(RegistrationDTO): User – регистрирует пользователя, возвращая доменную сущность.
ModelService
Создаёт экономические модели и осуществляет анализ документации. Методы:

createModel(ModelDTO, User): Model – создает модель для конкретного пользователя.
analyzeDocumentation(Document): AIAnalysisResult – отправляет документ для AI-анализа и возвращает результат.
CalculationService
Реализует логику вычислений. Метод:

performCalculation(Long, Map<String, Double>): CalculationResult – принимает идентификатор модели и параметры, выполняет расчёты, возвращая результат.
SimulationService
Выполняет имитационное моделирование. Метод:

performSimulation(SimulationRequestDTO): SimulationResult – запускает симуляцию на основе входных данных.
AIService
Инкапсулирует вызовы к внешнему AI-сервису. Методы:

analyzeDocument(Document): AIAnalysisResult – отправляет документ для анализа.
analyzeFinancialReport(FinancialReport): SimulationModel – анализирует финансовую отчетность и генерирует модель симуляции.
Сервисы получают данные из контроллеров (DTO), преобразуют их в доменные сущности, выполняют бизнес-логику и, при необходимости, обращаются к внешним системам (например, AI).

3. Data Access Layer (Spring Data JPA)
Этот слой представляет собой набор интерфейсов-репозиториев, которые предоставляют методы для работы с базой данных:

UserRepository – поиск пользователя по имени.
ModelRepository – поиск модели по идентификатору.
CalculationRepository – сохранение результатов расчётов.
SimulationRepository – сохранение результатов симуляций.
DocumentRepository – сохранение загруженных документов.
Эти репозитории автоматически реализуются Spring Data JPA и позволяют работать с данными через доменные сущности.

4. Domain Entities
Сущности, которые отражают структуру данных в базе данных:

User – данные пользователя (id, username, passwordHash, email, createdAt).
Model – экономическая модель, связывается с пользователем и содержит параметры.
Parameter – отдельный параметр модели.
CalculationResult – результат вычислений для модели, содержит карту результатов.
SimulationResult – результат симуляции с прогнозными данными.
Document – документ, загруженный для анализа (например, документация модели).
AIAnalysisResult – результат AI-анализа, содержащий извлеченные данные.
Эти классы используются для хранения информации в базе данных.

5. DTO Classes
Классы для обмена данными между клиентом и сервером. Они скрывают внутреннюю структуру доменных сущностей и формируют формат, удобный для передачи по сети:

AuthRequestDTO / AuthResponseDTO – для запросов и ответов при авторизации.
RegistrationDTO / UserDTO – для регистрации и представления данных пользователя.
ModelDTO / ParameterDTO – представление модели и её параметров.
CalculationRequestDTO / CalculationResultDTO – данные для расчётов и их результаты.
SimulationRequestDTO / SimulationResultDTO – данные для запуска симуляций и результаты.
DocumentDTO – информация о загружаемом документе.
AIAnalysisResultDTO – результаты, возвращаемые AI-сервисом.
6. External AI Service
AI_API
Представляет внешний сервис (например, OpenAI API), который используется для анализа документации или финансовой отчетности. Методы:
analyzeDocumentation(File): JSON
analyzeFinancialReport(File): JSON
AIService обращается к этому классу для получения результатов анализа, которые затем используются в бизнес-логике.

Взаимодействия между слоями
Контроллеры получают запросы от клиента в виде DTO и передают их в соответствующий сервис.
Сервисы обрабатывают запросы, используя логику приложения:
Выполняют операции над доменными сущностями.
Обращаются к репозиториям (Data Access Layer) для сохранения или извлечения данных.
При необходимости, вызывают методы AIService для анализа документов или финансовой отчётности.
Репозитории обеспечивают абстракцию работы с базой данных.
Результаты обработки возвращаются контроллерам в виде DTO и отправ